# Web frontend quality

- Preserve accessible names, keyboard navigation, focus order, and semantic HTML.
- Keep state local by default and introduce shared state only with a documented need.
- Test user-visible behavior rather than component internals.
- Use the package manager and framework conventions already approved by the repository.
